import { useContext } from "react";
import { AuthContext } from "../../providers/AuthProvider";

const UserInfo = () => {
    
    return (
        <div>
            
        </div>
    );
};

export default UserInfo